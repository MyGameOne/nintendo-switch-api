{
  "name": "nintendo-switch-api",
  "version": "0.0.0",
  "private": true,
  "description": "Nintendo Switch API on Cloudflare Workers",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.0.0",
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "eslint": "^9.32.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.26.1"
  }
}
